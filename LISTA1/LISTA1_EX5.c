#include <stdio.h>

void main(){
    float price;
    int aux;

    printf("How many chickens currently? ");
    scanf("%d", &aux);

    price = (aux * 11.5);
    printf("Final Cost: %.2f", %price);
}